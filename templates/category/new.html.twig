{% extends 'base.html.twig' %}
{% block title %}{{ parent() }} - Ajout d'une catégorie{% endblock %}
{% block body %}
    <h1 class="text-shadow-lg shadow-red font-sans text-4xl text-center font-bold uppercase">Création d'une nouvelle catégorie</h1>
    <section class="flex justify-center items-center h-full">
        <div class="block p-6 rounded-lg shadow-lg bg-red max-w-xl">
            {{ include('/category/_form.html.twig') }}
            <div class="mt-5">
                {{ component('_button', {
                    buttonUrl: path('program_index'),
                    buttonText: 'Retour',
                }) }}
            </div>

        </div>
    </section>
{% endblock %}