<h1 class=
    "text-shadow-lg
shadow-red
font-sans
text-4xl
text-center
font-bold
uppercase">
    Salut !
</h1>

{#<p>To reset your password, please visit the following link</p>#}
<p class="text-sans">Pour réinitialiser votre mot de passe, veuillez visiter le lien suivant</p>
<a href="{{ url('forgot_password_reset_password', {token: resetToken.token}) }}">{{ url('forgot_password_reset_password', {token: resetToken.token}) }}</a>

<p>Ce lien expirera
    dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Cordialement !</p>
