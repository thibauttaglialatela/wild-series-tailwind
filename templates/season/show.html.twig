{% extends 'base.html.twig' %}

{% block title %}Season{% endblock %}

{% block body %}
    <h1>Season</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ season.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ season.number }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ season.year }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ season.description }}</td>
            </tr>
        </tbody>
    </table>

    {{ component('_button', {
        buttonUrl: path('program_season_show', {'programId': program.id, 'seasonId': season.id}),
        buttonText: 'Retour'
    }) }}
{{ component('_button', {
    buttonUrl: path('season_edit', {'id': season.id, 'program_id': program.id}),
    buttonText: 'Editer la saison'
}) }}

    {{ include('season/_delete_form.html.twig') }}
{% endblock %}
