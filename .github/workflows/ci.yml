name: CI

on:
  push:
    branches:
    - main

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - uses: php-actions/composer@v6
    - uses: docker://oskarstark/phpstan-ga
      with:
        args: analyse src/ --level=9
    

    # ... then your own project steps ...
