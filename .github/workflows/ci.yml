name: CI

on:
  push:
    branches:
    - main

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: install composer
      run: composer install
      
    - name: lauch phpstan analyse
      run: ./vendor/bin/phpstan analyse src --level 5
    # ... then your own project steps ...
