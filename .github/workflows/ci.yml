name: CI

on:
  push:
    branches:
    - main

jobs:
  build-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: install composer
      run: composer install
      
    - uses: OskarStark/phpstan-ga@1.7.0
    # ... then your own project steps ...
